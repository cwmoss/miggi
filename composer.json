{
    "autoload": {
        "psr-4": {
            "miggi\\": "src/"
        }
    },
    "name": "cwmoss/miggi",
    "scripts": {
        "test": [
            "vendor/bin/phpunit tests"
        ],
        "analyze": "vendor/bin/phpstan analyse --memory-limit 1G -l 5"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^1.10"
    },
    "require": {
        "vlucas/phpdotenv": "^5.6",
        "psr/log": "^1.0"
    }
}